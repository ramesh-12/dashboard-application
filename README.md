# dashboard-application
An Angular-based visualization module that displays district rankings using ApexCharts. The component fetches data from a JSON file, processes the values, and renders grouped bar charts with user-selectable ranking options. The UI uses Angular Material for layout, styling, and interactivity.
